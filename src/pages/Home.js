import React from "react";

function Home() {

    return (
        <section>
            <h2 style={styles.body}>This is the Home page!</h2>
        </section>
    );
}

export default Home;

const styles = {
    body: {
        textAlign: 'center',
    },
}