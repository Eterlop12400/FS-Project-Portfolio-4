import React from "react";

function Search() {

    return (
        <section>
            <h2 style={styles.body}>This is the Search page!</h2>
        </section>
    );
}

export default Search;

const styles = {
    body: {
        textAlign: 'center',
    },
}